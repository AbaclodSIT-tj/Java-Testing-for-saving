# Java-Testing-for-saving
Don't mind me doing practice just correct me if some logic are wrong

bruh so void gives with no return.
OOP rule in non static vs static calling
static  → no object needed
non-static → object needed

void → returns nothing
return type → must return value
